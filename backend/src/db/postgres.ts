// Postgres access placeholder.
// TODO: Import the shared Prisma client here when backend code needs database access.
// Example later:
// import { prisma } from "../../../src/lib/prisma";

export const postgres = {
  // TODO: Add database helpers or repositories as the backend takes shape.
};
