// Route registration placeholder.
// TODO: Register routes that read/write data through Postgres via Prisma.

export const routes = {
  // accounts: undefined,
  // transactions: undefined,
  // budgets: undefined,
};
